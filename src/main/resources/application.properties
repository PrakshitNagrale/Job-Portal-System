spring.application.name=jobportalsystem

spring.datasource.url = jdbc:mysql://127.0.0.1:3306/jobportaldb
spring.datasource.username = root
spring.datasource.password =prakshit01
spring.jpa.show-sql = true
spring.jpa.hibernate.ddl-auto = update

#update -> hibernate will automatically create the missing table & update db automatically
#validate -> it will not create/update tables in db ,it will only check db schema matches the entity mapping ,and throw error if  not
#create-drop ->Creates tables at startup and drops them when app stops.